# Prachi Portfolio

A static single-page portfolio. No framework, no build step.

## Running Locally

To run locally, execute the following command in this directory:

```bash
npx --yes serve -l 3000 .
```

## Deployment

Deploy via Vercel with the following settings:
- **Framework preset**: Other
- **Build command**: (leave empty)
- **Output directory**: `.`
